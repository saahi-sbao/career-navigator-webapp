'use client';

export default function Suggestions() {
  // This component's functionality depends on Server Actions, which are not
  // compatible with the static export (`output: 'export'`) configuration.
  // It has been disabled to allow the application to build.
  return null;
}

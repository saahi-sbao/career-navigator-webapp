# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0
  maxInstances: 10
  cpu: 1
  memoryMiB: 512

# This section enables specific Google Cloud integrations
# Only include these if you have the APIs enabled in Google Cloud Console
# google_maps_platform: {}
# secrets_manager: {}